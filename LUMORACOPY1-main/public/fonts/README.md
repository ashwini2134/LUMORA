Place licensed font files here so the app can serve them.

Expected filenames (update CSS if you use different names):
- Canela-Regular.woff2
- Canela-Bold.woff2
- PPEditorialNew-Regular.woff2
- PPEditorialNew-Bold.woff2

Notes:
- Obtain and include these fonts according to their licenses.
- After adding files, run `npm run build` or `next build` to verify.
- If you prefer to self-host different formats, update `src/app/globals.css` @font-face `src` paths accordingly.
